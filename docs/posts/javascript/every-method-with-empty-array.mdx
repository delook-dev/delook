---
title: every 매서드와 공허참 개념의 연관성
type: 'concept'
language: JavaScript
tags:
  - Javascript
dateModified: 2025.09.02
---

# every 매서드

- every 매서드는 콜백 함수를 통해 배열의 요소들이 조건을 만족하는지를 판별합니다.
- 모든 배열 요소가 참을 반환하면 true, 그렇지 않으면 false를 반환합니다.

```js
const isBelowThreshold = (currentValue) => currentValue < 40;

const array1 = [1, 30, 39, 29, 10, 13];
const array2 = [34, 50, 24, 10, 17, 77];

console.log(array1.every(isBelowThreshold)); // true
console.log(array2.every(isBelowThreshold)); // false
```

## 빈 배열과 every 매서드

빈 배열을 대상으로 every 매서드를 실행하면 true를 반환합니다.

```js
const array3 = [];

console.log(array3.every(isBelowThreshold)); // true
```

논리학의 '공허참'이라는 개념을 통해 이유를 알 수 있습니다.

#### 조건문

- 이 빈 상자의 모든 공은 파란색이다.
  = 만약 x가 이 상자 안의 공이라면, x는 파란색이다.

- 이 배열 안의 모든 요소가 조건을 만족한다.
  = 만약 x가 배열 안의 요소라면, x는 조건을 만족한다.

#### 거짓된 전제

- 상자가 비어있다.
- 배열이 비어있다.

위 논리 구조에서 'x가 상자 안의 공이다' 라는 전제는 거짓입니다.

논리학에서는 거짓인 전제로부터는 어떤 결론도 참이 되며, 이를 '공허참'이라고 합니다.

## every 매서드를 사용할 때에는

- every를 '배열의 모든 요소가 조건을 만족하는가?'로 이해하기 보다는,
  '배열에 조건을 만족하지 않는 요소가 있는가?'로 이해해야 합니다.
- 빈 배열을 의도하지 않은 이상 배열의 length를 체크해야 합니다.

```js
const foo = (arr) => {
	if (arr.length === 0) {
		throw new Error('this method requires at least one element');
	}
	if (arr.every(...)) {
		...
	}
}
```
